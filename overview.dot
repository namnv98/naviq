digraph RuleGraph {
  rankdir=LR;
  node [shape=box, fontsize=11, style=filled, fillcolor=lightyellow];
  for_locking_clause;
  opt_for_locking_clause;
  where_clause;
  columnref;
  index_params;
  index_elem_options;
  indirection_el;
  index_elem;
  indirection;
  opt_indirection;
  target_list;
  target_el;
  target_alias;
  opt_name_list;
  qualified_name;
  name;
  preparablestmt;
  insertstmt;
  insert_target;
  insert_rest;
  colid;
  a_expr;
  override_kind;
  a_expr_qual;
  insert_column_list;
  insert_column_item;
  opt_on_conflict;
  collabel;
  opt_conf_expr;
  returning_clause;
  bare_col_label;
  set_clause_list;
  set_clause;
  set_target;
  set_target_list;
  func_expr_windowless;
  selectstmt;
  select_with_parens;
  select_no_parens;
  select_clause;
  simple_select_intersect;
  with_clause;
  cte_list;
  common_table_expr;
  opt_materialized;
  opt_with_clause;
  all_or_distinct;
  opt_sort_clause;
  sort_clause;
  select_limit;
  opt_select_limit;
  opt_for_locking_clause -> for_locking_clause;
  where_clause -> a_expr;
  columnref -> indirection;
  columnref -> colid;
  index_params -> index_elem;
  indirection_el -> a_expr;
  index_elem -> func_expr_windowless;
  index_elem -> a_expr;
  index_elem -> colid;
  index_elem -> index_elem_options;
  indirection -> indirection_el;
  opt_indirection -> indirection_el;
  target_list -> target_el;
  target_el -> target_alias;
  target_el -> a_expr;
  target_el -> columnref;
  target_alias -> collabel;
  target_alias -> bare_col_label;
  qualified_name -> indirection;
  qualified_name -> colid;
  name -> colid;
  preparablestmt -> selectstmt;
  preparablestmt -> insertstmt;
  insertstmt -> insert_target;
  insertstmt -> insert_rest;
  insertstmt -> opt_on_conflict;
  insertstmt -> opt_with_clause;
  insertstmt -> returning_clause;
  insert_target -> qualified_name;
  insert_target -> colid;
  insert_rest -> selectstmt;
  insert_rest -> override_kind;
  insert_rest -> insert_column_list;
  a_expr -> a_expr_qual;
  insert_column_list -> insert_column_item;
  insert_column_item -> opt_indirection;
  insert_column_item -> colid;
  opt_on_conflict -> where_clause;
  opt_on_conflict -> set_clause_list;
  opt_on_conflict -> opt_conf_expr;
  opt_conf_expr -> where_clause;
  opt_conf_expr -> name;
  opt_conf_expr -> index_params;
  returning_clause -> target_list;
  set_clause_list -> set_clause;
  set_clause -> a_expr;
  set_clause -> set_target;
  set_clause -> set_target_list;
  set_target -> opt_indirection;
  set_target -> colid;
  set_target_list -> set_target;
  selectstmt -> select_with_parens;
  selectstmt -> select_no_parens;
  select_with_parens -> select_with_parens;
  select_with_parens -> select_no_parens;
  select_no_parens -> opt_sort_clause;
  select_no_parens -> select_clause;
  select_no_parens -> select_limit;
  select_no_parens -> with_clause;
  select_no_parens -> opt_select_limit;
  select_no_parens -> for_locking_clause;
  select_no_parens -> opt_for_locking_clause;
  select_clause -> all_or_distinct;
  select_clause -> simple_select_intersect;
  simple_select_intersect -> all_or_distinct;
  with_clause -> cte_list;
  cte_list -> common_table_expr;
  common_table_expr -> preparablestmt;
  common_table_expr -> opt_name_list;
  common_table_expr -> name;
  common_table_expr -> opt_materialized;
  opt_with_clause -> with_clause;
  opt_sort_clause -> sort_clause;
  opt_select_limit -> select_limit;
}
